//not AC
#include <bits/stdc++.h>
using namespace std;
bool flag = false;
char trans(int n)
{
    if (0 <= n && n <= 9)
        return (char)(n + '0');
    else
        return (char)(n - 10 + 'A');
}
void print(int n, int r)
{
    int res;
}
int main()
{
    int n, r;
    cin >> n >> r;
    print(n, r);
    return 0;
}